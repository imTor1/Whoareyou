<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_pressed="true">
        <shape android:shape="rectangle">
            <solid android:color="@color/purple_500" /> <!-- สีเมื่อกดปุ่ม -->
            <corners android:radius="16dp" /> <!-- มุมมน -->
        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/purple_200" /> <!-- สีปกติ -->
            <corners android:radius="16dp" /> <!-- มุมมน -->
        </shape>
    </item>
</selector>
